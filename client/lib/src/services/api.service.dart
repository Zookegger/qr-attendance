class ApiService {
  // Example API service - replace with real implementation
  Future<String> fetchWelcome() async {
    await Future.delayed(const Duration(milliseconds: 200));
    return 'Welcome from ApiService';
  }
}
